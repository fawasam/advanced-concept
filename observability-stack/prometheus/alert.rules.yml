groups:
  - name: node-alerts
    rules:
      - alert: HighRequestLatency
        expr: http_request_duration_seconds_bucket > 1
        for: 1m
        labels:
          severity: critical
        annotations:
          summary: "High request latency"
